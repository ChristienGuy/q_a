name: Deploy API
on:
  push:
    branches:
      - master
    # paths:
    #   - "api/**"

jobs:
  build:
    name: build api
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: set remote
        run: git remote add heroku https://heroku:${{secrets.HEROKU_API_KEY}}@git.heroku.com/question-answer.git
      - run: git subtree push --prefix api heroku master
